---
import { navigation } from '../data/site';
---

<header class="site-header">
  <div class="container">
    <div class="brand">
      <span class="brand-mark">Chronicle Of Xiaonuan</span>
      <strong class="brand-title">枭暖史书</strong>
    </div>
    <nav class="nav">
      {navigation.map((item) => (
        <a href={item.href}>{item.label}</a>
      ))}
    </nav>
  </div>
</header>
